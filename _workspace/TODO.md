# HomeStyler 남은 작업 (TODO)

v1 P1 개발(M0~M5)은 완료·QA 통과. 아래는 그 이후 남은 작업을 한곳에 모은 목록. 출처: `docs/srs.md` §7 백로그 + `progress.md` 미구현/Mock 누적 + QA 알려진 이슈.

## A. 실서비스 전환 필수 (P1 데모 → 운영)

- [ ] **실제 AI 연동** — 지금 전부 Mock. `AiAnalysisService`(M3 분석/추천)·`AiEstimationService`(M2 치수·가구 추정)·이미지 생성을 실제 AI 어댑터로 교체. 인터페이스는 이미 분리돼 있음
- [ ] **PostgreSQL 전환** — dev는 H2 인메모리(재시작 시 데이터 소멸). prod 프로파일 + PostgreSQL, `ddl-auto: validate`, 마이그레이션(Flyway 등)
- [ ] **파일 저장 오브젝트 스토리지화** — `FileStorageService` 로컬 구현을 S3 등으로. 서명 URL·at-rest 암호화(NFR-SEC-001)
- [ ] **prod 보안 설정 분리** — JWT 시크릿 환경변수화, `/h2-console` permitAll 제거, CORS 도메인 확정

## B. 미구현 기능 (범위였으나 조건부 보류)

- [ ] **소셜 로그인(FR-AUTH-001)** — 현재 버튼만. 카카오/네이버/Apple/Google OAuth. 소셜 키 확보 후
- [ ] **HOME `recentSpaces` 실데이터 연결** — 현재 빈 배열 고정. M2 Space 엔티티와 연결(백엔드 간단 작업)
- [ ] **FR-SPACE-008 이미지 품질 검증** — 현재 스텁(항상 PASSED). 블러/저조도/공간 인식 실판정

## C. 알려진 이슈 (비차단)

- [ ] **깨진/비UTF-8 JSON 본문 → 400** — 현재 500 반환. `GlobalExceptionHandler`에 `HttpMessageNotReadableException`→COMMON_400 핸들러 추가. (이미 별도 백그라운드 태스크로 분리됨)
- [ ] **미존재 `/files/{name}` GET → 404** — 현재 500. 정상 흐름 밖이라 낮은 우선순위

## D. 제품 백로그 (srs.md §7, 외부 데이터·제휴 확보 후)

- [ ] **BL-001 실제 판매 제품 연동** — Mock 상품 → 실시간 가격·재고·구매링크·대체품. 상품 스키마는 이미 실데이터 교체 가능 형태. *선행: 커머스 제휴/상품 API*
- [ ] **BL-002 반입 가능성 검사** — 현관·복도·엘리베이터 치수 기반 대형 가구 반입 판정(현재 체크리스트 문구로 대체). *선행: 반입 경로 데이터*
- [ ] **BL-003 시공업체 견적서·중개** — 견적 요청서 자동 생성·업체 연결(현재 구매목록 내보내기까지). *선행: 거래 데이터·업체 품질관리*
- [ ] **BL-004 한국 아파트 도면 DB** — 단지·평형별 표준 도면 자동 매칭. *선행: 도면 소싱, 경쟁사 대비 실익 검토*
- [ ] **BL-005 결제·과금 모델** — 공간 단위 건별 결제, 제휴 수수료. *선행: MVP 지표 검증*
- [ ] **BL-006 물리 기반 적합도 고도화** — 룰 기반(FR-RECO-015)을 3D 충돌·동선 시뮬레이션으로. *선행: 3D 모델링 파이프라인*

## E. 범위 외로 확정 (참고)

- 관리자 백오피스(ADM), 위시리스트(FR-SAVE-005 P3), 데이터 다운로드(FR-DATA-005 P3), 다중 집(Home) 관리 — 요청 시 하네스 확장으로 처리
